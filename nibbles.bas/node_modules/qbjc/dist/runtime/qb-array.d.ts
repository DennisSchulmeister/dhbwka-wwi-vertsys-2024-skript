import { ArrayDimensionSpec, ArrayTypeSpec, SingularTypeSpec } from '../lib/types';
/** Runtime representation for QBasic arrays. */
declare class QbArray {
    constructor(arrayTypeSpec: ArrayTypeSpec);
    /** Initializes the array with the provided spec, discarding any previous contents. */
    init(elementTypeSpec: SingularTypeSpec, dimensionSpecs: Array<ArrayDimensionSpec>): void;
    /** Translates a QBasic index into the index in the underlying array. */
    getIdx(dimensionIdx: number, qbIdx: number): number;
    get values(): any[];
    get typeSpec(): ArrayTypeSpec;
    /** The underlying array storing the actual elements. */
    private _values;
    /** Current type spec. */
    private _typeSpec;
}
export default QbArray;
