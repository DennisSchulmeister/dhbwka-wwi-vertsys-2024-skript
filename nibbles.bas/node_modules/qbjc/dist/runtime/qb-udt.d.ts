import { UdtTypeSpec } from '../lib/types';
/** Runtime representation for QBasic user-defined types. */
declare class QbUdt {
    constructor(udtTypeSpec: UdtTypeSpec);
    private init;
    clone(): QbUdt;
    get values(): {
        [key: string]: any;
    };
    get typeSpec(): UdtTypeSpec;
    /** The underlying array storing the actual elements. */
    private _values;
    /** Current type spec. */
    private _typeSpec;
}
export default QbUdt;
