import { Terminal } from 'xterm';
import AnsiTerminalPlatform from './ansi-terminal-platform';
import { CompiledModule } from './compiled-code';
import Executor, { ExecutionOpts } from './executor';
/** RuntimePlatform for the browser environment based on xterm.js. */
export declare class BrowserPlatform extends AnsiTerminalPlatform {
    private readonly terminal;
    constructor(terminal: Terminal);
    defaultStmtExecutionDelayUs: number;
    loadCompiledModule(code: string): Promise<CompiledModule>;
    delay(delayInUs: number): Promise<void>;
    print(s: string): Promise<void>;
    inputLine(): Promise<string>;
    getChar(): Promise<string | null>;
    getCursorPosition(): Promise<{
        x: number;
        y: number;
    }>;
    getScreenSize(): Promise<{
        rows: number;
        cols: number;
    }>;
}
export declare class BrowserExecutor extends Executor {
    constructor(terminal: Terminal, opts?: ExecutionOpts);
}
