import { Parser, ParserOptions } from 'nearley';
import { Module } from '../lib/ast';
export declare function createParser(opts?: ParserOptions): Parser;
/** Entry point for parsing an input string into an AST module. */
export default function parse(input: string, opts?: {
    sourceFileName?: string;
} & ParserOptions): Module;
