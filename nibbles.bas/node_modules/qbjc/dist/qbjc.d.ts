#!/usr/bin/env node
import { CompileArgs, CompileResult } from './compile';
export interface CompileFileArgs extends Omit<CompileArgs, 'source' | 'sourceFileName'> {
    sourceFilePath: string;
    outputFilePath?: string;
    enableSourceMap?: boolean;
}
export interface CompileFileResult extends CompileResult {
    source: string;
    outputFilePath: string;
}
/** Compiles a QBasic source file and write out the compiled program and source map. */
export declare function compileFile({ sourceFilePath, outputFilePath: outputFilePathArg, enableSourceMap, enableBundling, enableMinify, }: CompileFileArgs): Promise<CompileFileResult>;
