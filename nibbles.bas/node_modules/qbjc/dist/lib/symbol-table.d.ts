import { DataTypeSpec } from './types';
/** Type of a variable symbol. */
export declare enum VarType {
    /** Regular variable. */
    VAR = "var",
    /** Static variable. */
    STATIC_VAR = "staticVar",
    /** Constant. */
    CONST = "const",
    /** FUNCTION or SUB argument. */
    ARG = "arg"
}
/** Scope of a variable symbol. */
export declare enum VarScope {
    LOCAL = "local",
    GLOBAL = "global"
}
interface VarSymbolBase {
    name: string;
    typeSpec: DataTypeSpec;
}
interface LocalVarSymbol extends VarSymbolBase {
    varType: VarType.STATIC_VAR | VarType.ARG;
    varScope: VarScope.LOCAL;
}
interface LocalOrGlobalVarSymbol extends VarSymbolBase {
    varType: VarType.VAR | VarType.CONST;
    varScope: VarScope.LOCAL | VarScope.GLOBAL;
}
/** A variable in a symbol table. */
export declare type VarSymbol = LocalVarSymbol | LocalOrGlobalVarSymbol;
/** Symbol table for variables. */
export declare type VarSymbolTable = Array<VarSymbol>;
export declare function lookupSymbol<T extends {
    name: string;
}>(symbolTable: Array<T>, name: string): T | null;
export declare function lookupSymbols<T extends {
    name: string;
}>(symbolTable: Array<T>, name: string): T[];
export {};
