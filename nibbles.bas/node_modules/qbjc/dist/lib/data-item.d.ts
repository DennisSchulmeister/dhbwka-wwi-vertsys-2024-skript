import { DataTypeSpec } from './types';
/** A constant defined by a DATA statement.
 *
 * The format is [[line, col], value].
 *
 * We use this shortened form instead of JSON due to the highly repetitive nature of these items.
 */
export declare type DataItem = [[number, number], DataItemValue];
/** A sequence of DataItems. */
export declare type DataItems = Array<DataItem>;
/** Actual value of a DataItem. */
export declare type DataItemValue = number | string | null;
export declare function dataItemTypeName([_, value]: DataItem): "string" | "number" | "bigint" | "boolean" | "symbol" | "undefined" | "object" | "function" | "null";
/** Converts a data item to the expected type. */
export declare function getDataItem(item: DataItem, expectedTypeSpec: DataTypeSpec): string | number;
